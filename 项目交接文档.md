# 电力风险智能交底系统 - 项目交接文档

> **项目名称**：电力作业风险辨识与安全交底Agent
> **最新更新**：2026-07-21
> **技术栈**：Spring Boot 3.2.5 + Java 17 + MySQL 8.0 + 原生HTML/CSS/JS

---

## 一、项目概述

### 1.1 项目简介

本项目是一个电力作业风险辨识与安全交底的全栈应用，实现电力作业场景下的智能化风险识别、安全交底流程管理。核心能力包括：

- 🎯 **图像风险识别**：基于腾讯云TIIA + 本地YOLO双模式的人员/PPE/火焰检测
- 🌤️ **气象风险评估**：城市天气查询 + 电力作业气象衍生风险计算
- 📋 **安全交底管理**：任务解析、危险点识别、安全措施生成
- 📝 **事故案例库**：60+真实电力事故案例，支持分类检索
- 🔐 **用户权限体系**：JWT认证 + Spring Security + BCrypt加密

### 1.2 项目定位

这是一个**前后端一体化**的Spring Boot项目，前端静态资源直接放在`src/main/resources/static`目录下，由Spring Boot统一提供服务，无需单独部署前端。

---

## 二、技术栈详解

### 2.1 后端技术栈

| 分类 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 核心框架 | Spring Boot | 3.2.5 | 主框架，Java 17 |
| 安全框架 | Spring Security | 6.2.x | 随Spring Boot自动引入 |
| ORM框架 | MyBatis-Plus | 3.5.6 | 简化数据库操作 |
| 数据库 | MySQL | 8.0+ | 主数据库 |
| JWT认证 | JJWT | 0.12.5 | Token生成与校验 |
| 图像处理 | JavaCV | 1.5.10 | OpenCV Java封装，图像检测 |
| 腾讯云SDK | tencentcloud-sdk-java | 3.1.920 | TIIA PPE检测 |
| 文档处理 | Apache POI | 5.2.5 | Word文档生成 |
| 文档处理 | iText Core | 7.2.5 | PDF文档生成 |
| JSON处理 | FastJSON | 1.2.83 | JSON序列化 |
| AI框架 | LangChain4j | 0.34.0 | AI Agent框架（DeepSeek对接） |

### 2.2 前端技术栈

| 分类 | 技术 | 说明 |
|------|------|------|
| 页面 | 原生HTML5 | 无前端构建工具，直接静态页面 |
| 样式 | CSS3 + 自定义主题 | pc-style.css为主样式 |
| 交互 | 原生JavaScript (ES6+) | 无框架，直接操作DOM |
| 图表 | 原生Canvas | 检测结果绘制 |
| HTTP | Fetch API | 前后端通信 |
| 认证 | localStorage存Token | 请求头带Authorization |

### 2.3 设计规范

- **后端包命名**：`org.example.eledangercheck`
- **Java版本**：Java 17（注意：使用`javax.*`而非`jakarta.*`命名空间）
- **数据库表**：全部使用`CREATE TABLE IF NOT EXISTS`和`INSERT IGNORE INTO`，保证重启不丢数据
- **安全配置**：`server.address=0.0.0.0`支持局域网访问
- **默认端口**：8081

---

## 三、项目目录结构

```
ELeDangerCheck/
├── src/main/java/org/example/eledangercheck/
│   ├── config/                    # 配置类（14个）
│   │   ├── SecurityConfig.java          # Spring Security配置
│   │   ├── DatabaseInitializer.java     # 数据库自动建表+初始化数据
│   │   ├── TencentTiiaConfig.java       # 腾讯云TIIA配置
│   │   ├── DeepSeekConfig.java          # DeepSeek AI配置
│   │   ├── AliyunTtsConfig.java         # 阿里云TTS配置
│   │   ├── EsignConfig.java             # e签宝配置
│   │   ├── ElasticsearchConfig.java     # ES配置（可选）
│   │   ├── SimulationConfig.java        # 模拟模式配置
│   │   ├── WebConfig.java               # Web MVC配置
│   │   ├── RestTemplateConfig.java      # RestTemplate配置
│   │   ├── GlobalExceptionHandler.java  # 全局异常处理
│   │   ├── DroolsConfig.java            # Drools规则引擎（占位）
│   │   ├── FastChatConfig.java          # FastChat配置
│   │   └── CityDataInitializer.java     # 城市数据初始化
│   │
│   ├── controller/                # 控制器（14个）
│   │   ├── AuthController.java          # 认证：注册/登录
│   │   ├── ImageAnalysisController.java # 图像检测API
│   │   ├── WeatherRiskController.java   # 天气风险API
│   │   ├── TaskController.java          # 作业任务管理
│   │   ├── HazardController.java        # 危险点管理
│   │   ├── SafetyMeasureController.java # 安全措施管理
│   │   ├── DisclosureController.java    # 安全交底管理
│   │   ├── AccidentCaseController.java  # 事故案例管理
│   │   ├── DictController.java          # 字典管理
│   │   ├── AgentController.java         # AI Agent对话
│   │   ├── TtsController.java           # 语音播报
│   │   ├── DocumentController.java      # 文档下载
│   │   ├── EsignController.java         # 电子签章
│   │   └── EvaluationController.java    # 质量评估
│   │
│   ├── service/                   # 业务服务（20个）
│   │   ├── OpenCvDetectionService.java  # 核心检测调度（腾讯云+本地）
│   │   ├── ImageAnalysisService.java    # 图像场景分析
│   │   ├── YoloDetectionService.java    # YOLO11x通用目标检测
│   │   ├── PpeDetectionService.java     # 本地PPE检测（4类）
│   │   ├── WeatherRiskService.java      # 天气风险计算
│   │   ├── UserService.java             # 用户服务
│   │   ├── TaskService.java             # 任务服务
│   │   ├── HazardService.java           # 危险点服务
│   │   ├── SafetyMeasureService.java    # 安全措施服务
│   │   ├── DisclosureService.java       # 交底服务
│   │   ├── AccidentCaseService.java     # 事故案例服务
│   │   ├── DictService.java             # 字典服务
│   │   ├── AgentService.java            # Agent调度
│   │   ├── AiAgentService.java          # AI Agent逻辑
│   │   ├── RuleEngineService.java       # 规则引擎
│   │   ├── TtsService.java              # 语音合成
│   │   ├── DocumentService.java         # 文档生成
│   │   ├── DownloadService.java         # 文件下载
│   │   ├── EsignService.java            # 电子签章
│   │   └── EvaluationService.java       # 质量评估
│   │
│   ├── entity/                    # 实体类（14个）
│   ├── mapper/                    # MyBatis Mapper（12个）
│   ├── dto/                       # 数据传输对象（3个）
│   ├── security/                  # JWT认证过滤器和Provider
│   ├── util/                      # 工具类（4个）
│   │   ├── TencentPpeDetectUtil.java    # 腾讯云PPE检测工具
│   │   ├── PpeResultParseUtil.java      # PPE结果解析
│   │   ├── JwtUtil.java                 # JWT工具
│   │   └── FileToBase64Util.java        # 文件转Base64
│   ├── exception/                 # 业务异常
│   └── ELeDangerCheckApplication.java   # 启动类
│
├── src/main/resources/
│   ├── static/                    # 前端静态资源
│   │   ├── css/
│   │   │   ├── style.css               # 基础样式
│   │   │   ├── pc-style.css            # PC端主样式
│   │   │   └── animations.css          # 动画
│   │   ├── js/
│   │   │   ├── login.js                # 登录页逻辑
│   │   │   ├── register.js             # 注册页逻辑
│   │   │   ├── dashboard.js            # 控制台（主页面）
│   │   │   ├── image-risk.js           # 图像风险检测
│   │   │   ├── weather-risk.js         # 天气风险评估
│   │   │   ├── profile.js              # 个人中心
│   │   │   └── disclosure-detail.js    # 交底详情
│   │   ├── index.html              # 登录页
│   │   ├── register.html           # 注册页
│   │   ├── dashboard.html          # 控制台（主管理页）
│   │   ├── image-risk.html         # 图像风险检测页
│   │   ├── weather-risk.html       # 天气风险页
│   │   ├── accident-cases.html     # 事故案例页
│   │   ├── chat.html               # AI对话页
│   │   ├── profile.html            # 个人中心
│   │   └── disclosure-detail.html  # 交底详情页
│   │
│   ├── application.yml             # 主配置（默认profile=mysql）
│   ├── application-mysql.yml       # MySQL环境配置
│   └── schema.sql                  # 建表脚本（备用）
│
├── doc/schema.sql                  # 数据库脚本
├── pom.xml                         # Maven依赖
└── mvnw/mvnw.cmd                   # Maven Wrapper
```

---

## 四、核心功能模块详解

### 4.1 图像风险识别模块（核心新增）

**入口页面**：`image-risk.html`  
**API前缀**：`/api/vision`

#### 功能组成

1. **图像预处理**
   - 亮度、清晰度计算
   - 主色调提取
   - OpenCV实现

2. **PPE安全装备检测（双模式）**
   - **腾讯云TIIA模式**（优先）：DetectSecurity接口
     - 检测项：安全帽、反光衣、安全带、抽烟、玩手机
     - 精度高，需联网
     - 配置：`tencent.tiia.enabled=true`
   - **本地YOLO模式**（兜底）：PpeDetectionService
     - 检测项：Helmet/NoHelmet/Vest/NoVest（4类）
     - 模型：best.onnx（需自行放置）
     - 云API失败时自动降级

3. **人员检测**
   - YOLO11x通用检测（COCO 80类）
   - 或HOG+SVM传统方法兜底

4. **火焰检测**
   - HSV颜色空间 + 形态学处理
   - 误过滤：排除红色非火焰物体

5. **场景分析**
   - DeepSeek AI分析（可选，模拟模式兜底）
   - 违章识别（未戴安全帽、未穿反光衣等）

#### 关键代码链路

```
用户上传图片
  ↓
ImageAnalysisController.analyzeScene()
  ↓
ImageAnalysisService.analyzeSceneImage()
  ├─ preprocessWithOpenCV()    // 图像预处理
  ├─ OpenCvDetectionService.detectAll()  // 目标检测
  │   ├─ detectPpeWithTencent()  // 优先腾讯云PPE
  │   ├─ YoloDetectionService    // YOLO人员检测（兜底）
  │   ├─ PpeDetectionService     // 本地PPE检测（兜底）
  │   └─ detectFire()            // 火焰检测
  └─ analyzeWithDeepSeek()      // AI场景分析（可选）
```

#### 检测结果数据结构

```javascript
detection: {
  persons: [{x, y, width, height, confidence}],
  helmets: [{x, y, width, height, confidence}],
  vests: [{x, y, width, height, confidence}],
  fires: [{x, y, width, height, confidence}],
  personCount: 0,
  helmetCount: 0,
  vestCount: 0,
  fireCount: 0,
  noHelmetCount: 0,
  noVestCount: 0,
  ppeSource: "tencent" | "local",  // PPE检测来源
  ppeRequestId: "xxx"               // 腾讯云请求ID
}
```

---

### 4.2 气象风险评估模块

**入口页面**：`weather-risk.html`  
**API前缀**：`/api/weather`

#### 功能组成

1. **城市天气查询**
   - 数据源：itboy天气API（免费接口）
   - 后端代理请求，避免前端CORS问题
   - 支持城市搜索（weather_city表）

2. **天气数据展示**
   - 实时天气：温度、湿度、空气质量
   - 7日预报
   - 日出日落、风力风向

3. **电力作业气象风险计算**
   - 动态风险等级评估
   - 作业限制建议
   - 受影响工种提示
   - 关联事故案例

4. **气象预警提示**
   - 高温、暴雨、大风等预警
   - 作业禁止/限制建议

#### 关键API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/weather/search?keyword=` | GET | 城市搜索 |
| `/api/weather/city/{cityCode}` | GET | 城市天气数据 |
| `/api/weather/report/{cityCode}` | GET | 完整报告（含风险计算） |

---

### 4.3 控制台（主管理页）

**入口页面**：`dashboard.html` + `dashboard.js`

#### 左侧导航菜单

1. **首页概览** - 数据统计卡片、最近任务
2. **作业任务** - 任务CRUD、任务解析
3. **危险点管理** - 危险点列表、关联措施
4. **安全交底** - 交底卡列表、生成/签名
5. **图像风险检测** - 跳转image-risk.html
6. **天气风险评估** - 跳转weather-risk.html
7. **事故案例** - 60+案例检索、分类筛选
8. **AI智能对话** - 跳转chat.html
9. **字典管理** - 危险源、措施字典
10. **个人中心** - 跳转profile.html

#### 模块切换机制

使用CSS类`.module-content.active`控制模块显隐，JS通过`switchModule(moduleName)`函数切换，避免页面跳转。

---

### 4.4 事故案例库

**入口页面**：`accident-cases.html`（独立页） + dashboard中cases模块

#### 数据规模
- 60+条真实电力事故案例
- 分类：触电、高处坠落、误操作、火灾、机械伤害、坍塌、物体打击等
- 字段：案例名称、事故类型、电压等级、作业类型、设备类型、严重程度、经过、原因分析、教训、危险点

---

### 4.5 安全交底流程

1. **任务创建** - 输入作业任务描述
2. **任务解析** - 提取电压等级、设备类型、作业类型等
3. **危险点识别** - 基于规则 + 案例匹配
4. **安全措施生成** - 对应危险点的控制措施
5. **交底卡生成** - Word/PDF文档
6. **交底签名** - 电子签章（模拟模式可用）
7. **质量评估** - 自动评分

---

## 五、数据库设计

### 5.1 数据表清单（共12张）

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `user` | 用户表 | id, username, password, real_name, role, department |
| `task` | 作业任务表 | id, task_name, task_type, voltage_level, status |
| `task_parse` | 任务解析表 | id, task_id, voltage_level, equipment_type |
| `hazard` | 危险点表 | id, task_id, hazard_name, category, hazard_level |
| `safety_measure` | 安全措施表 | id, hazard_id, measure_name, priority |
| `disclosure` | 安全交底表 | id, task_id, title, content, pdf_path, word_path, sign_status |
| `disclosure_sign` | 交底签名表 | id, disclosure_id, user_id, sign_image |
| `accident_case` | 事故案例表 | id, case_name, accident_type, severity, case_desc |
| `hazard_dict` | 危险点字典 | id, dict_code, dict_name, level, priority |
| `measure_dict` | 措施字典 | id, dict_code, dict_name, hazard_code |
| `evaluation` | 评估结果表 | id, disclosure_id, score, evaluation_desc |
| `weather_city` | 城市表 | id, city_code, city_name, province |

### 5.2 初始化机制

- 由`DatabaseInitializer.java`在启动时自动执行
- 所有表使用`CREATE TABLE IF NOT EXISTS`
- 初始数据使用`INSERT IGNORE INTO`，不会覆盖已有数据
- **重要**：无`DROP TABLE`语句，重启不丢失用户数据

---

## 六、API接口清单

### 6.1 认证接口（无需Token）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录，返回JWT Token |
| GET | `/api/auth/health` | 健康检查 |

### 6.2 图像检测接口（需Token）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/vision/analyze-scene` | 完整场景分析（预处理+检测+AI分析） |
| POST | `/api/vision/preprocess` | 仅图像预处理 |
| POST | `/api/vision/detect` | 仅目标检测 |

### 6.3 天气风险接口（无需Token）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/weather/search?keyword=` | 城市搜索 |
| GET | `/api/weather/city/{cityCode}` | 天气数据 |
| GET | `/api/weather/report/{cityCode}` | 完整风险报告 |

### 6.4 业务管理接口（需Token）

| 模块 | 前缀 | 典型接口 |
|------|------|---------|
| 任务管理 | `/api/tasks` | GET /list, POST /create, PUT /{id}, DELETE /{id} |
| 危险点 | `/api/hazards` | GET /task/{taskId}, POST /create |
| 安全措施 | `/api/safety-measures` | GET /hazard/{hazardId} |
| 安全交底 | `/api/disclosures` | GET /list, POST /generate, GET /{id} |
| 事故案例 | `/api/accident-cases` | GET /list, GET /{id}, GET /search |
| 字典管理 | `/api/dict` | GET /hazards, GET /measures |
| Agent | `/api/agent` | POST /complete-flow, POST /parse |
| 语音播报 | `/api/tts` | POST /synthesize |
| 文档下载 | `/api/document` | GET /{id}/word, GET /{id}/pdf |
| 电子签章 | `/api/esign` | POST /sign |
| 质量评估 | `/api/evaluation` | POST /disclosure/{id} |

---

## 七、配置文件详解

### 7.1 application.yml（主配置）

```yaml
server:
  port: 8081
  address: 0.0.0.0        # 支持局域网访问

spring:
  profiles:
    active: mysql          # 激活MySQL环境

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true   # 下划线转驼峰
  global-config:
    db-config:
      id-type: auto         # 主键自增

jwt:
  secret: ${JWT_SECRET:}     # 真实值放 secrets-local.yml 或环境变量
  expiration: 86400000      # 24小时

tencent:
  tiia:
    secret-id: ${TENCENT_TIIA_SECRET_ID:}
    secret-key: ${TENCENT_TIIA_SECRET_KEY:}
    region: ap-guangzhou
    enabled: true           # 腾讯云PPE检测开关
```

### 7.2 application-mysql.yml（MySQL配置）

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eledangercheck?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8
    username: root
    password: ${SPRING_DATASOURCE_PASSWORD:}

simulation:
  enabled: true             # 全局模拟模式
  tts-enabled: true
  esign-enabled: true
  deepseek-enabled: true
  fastchat-enabled: true

deepseek:
  api-key: ${DEEPSEEK_API_KEY:}
  base-url: https://api.deepseek.com/v1

aliyun:
  tts:
    access-key-id: ${ALIYUN_ACCESS_KEY_ID:}
    access-key-secret: ${ALIYUN_ACCESS_KEY_SECRET:}
    region-id: cn-hangzhou
    voice-name: Aiyun

esign:
  app-id: ${ESIGN_APP_ID:}
  app-secret: ${ESIGN_APP_SECRET:}
  api-url: https://openapi.esign.cn

elasticsearch:
  enabled: false            # ES默认关闭
  host: localhost
  port: 9200
```

### 7.3 安全配置说明

**SecurityConfig.java** 放行规则：

- 静态资源：`/`, `/css/**`, `/js/**`, `/*.html` 全部放行
- API：`/api/auth/**`、`/api/weather/**` 放行
- 其他API需JWT认证
- CSRF关闭（纯API场景）
- CORS允许所有来源（开发环境）
- 会话策略：STATELESS（无状态JWT）

---

## 八、第三方服务集成

### 8.1 腾讯云TIIA PPE检测（已配置）

- **接口**：DetectSecurity
- **检测项**：安全帽、反光衣、安全带、抽烟、玩手机
- **优先级**：高于本地YOLO
- **失败降级**：自动切换到本地PpeDetectionService
- **配置位置**：application.yml 的 `tencent.tiia` 节点

### 8.2 DeepSeek AI（部分配置）

- **用途**：图像场景分析文本描述
- **模式**：模拟模式已开启，返回本地规则生成的描述
- **真实使用**：关闭simulation.deepseek-enabled并配置有效API Key

### 8.3 阿里云TTS（模拟模式）

- **用途**：语音播报交底内容
- **当前状态**：模拟模式，不产生真实调用

### 8.4 e签宝（模拟模式）

- **用途**：电子签名
- **当前状态**：模拟模式，本地Canvas手写签名图片保存

### 8.5 天气API（itboy）

- **数据源**：http://t.weather.itboy.net/api/weather/city/
- **特点**：免费、无需Key、后端代理避免CORS
- **无需配置**

---

## 九、启动与部署

### 9.1 环境要求

- JDK 17+
- MySQL 8.0+
- Maven 3.8+（或用mvnw）

### 9.2 数据库准备

```sql
CREATE DATABASE eledangercheck DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

表会在首次启动时自动创建。

### 9.3 启动方式

```bash
# 方式1：Maven启动
cd ELeDangerCheck
./mvnw spring-boot:run

# 方式2：打包后运行
./mvnw clean package
java -jar target/ELeDangerCheck-0.0.1-SNAPSHOT.jar

# 方式3：指定profile
java -jar target/ELeDangerCheck-0.0.1-SNAPSHOT.jar --spring.profiles.active=mysql
```

### 9.4 访问地址

| 页面 | URL |
|------|-----|
| 登录页 | http://localhost:8081/index.html |
| 注册页 | http://localhost:8081/register.html |
| 控制台 | http://localhost:8081/dashboard.html |
| 图像检测 | http://localhost:8081/image-risk.html |
| 天气风险 | http://localhost:8081/weather-risk.html |
| 事故案例 | http://localhost:8081/accident-cases.html |
| AI对话 | http://localhost:8081/chat.html |
| 个人中心 | http://localhost:8081/profile.html |

### 9.5 默认账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | 123456 | admin |
| 111 | 123456 | operator |

---

## 十、开发注意事项与坑点

### 10.1 已知约束

1. **Java命名空间**：使用`javax.*`（如`javax.sql.DataSource`），不要用`jakarta.*`
2. **模型文件不入库**：ONNX模型文件较大，.gitignore已排除`*.onnx`和`models/`目录
3. **数据库无DROP**：DatabaseInitializer严禁写DROP TABLE，保护用户数据
4. **前端无构建**：纯静态页面，直接修改HTML/CSS/JS即可，无需npm/webpack
5. **模块显隐**：用CSS类`.module-content.active`控制，不要用inline的display:none

### 10.2 图像检测注意事项

1. **双模式降级**：腾讯云失败自动走本地YOLO，不要假设一定走云端
2. **PPE结果结构**：腾讯云和本地检测结果通过PpeDetectionResult统一封装
3. **letterbox预处理**：YOLO推理前要做letterbox保持宽高比，不要直接resize
4. **数据布局**：OpenCV DNN读取ONNX用channel-first（NCHW），不是anchor-first
5. **安全标识已删除**：当前版本已移除安全标识检测，仅保留人员/PPE/火焰

### 10.3 前端开发注意

1. **事件绑定**：动态按钮用`data-*`属性 + 事件委托，不要用inline onclick
2. **AbortController**：每个模块独立的AbortController，不要全局共享导致请求中断
3. **Token存储**：localStorage存token，请求头加`Authorization: Bearer <token>`
4. **Canvas绘制**：检测结果绘制在canvas上，要计算图像显示区域坐标映射

### 10.4 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 启动后访问403 | Spring Security拦截 | 检查SecurityConfig放行路径 |
| 图像检测失败 | 模型文件缺失 | 放置yolo11x.onnx到resources/models/ |
| 腾讯云PPE不返回 | 密钥错误/无权限 | 检查SecretId/SecretKey，确认子账号有TIIA权限 |
| 天气数据加载失败 | itboy接口不稳定 | 后端代理重试，或换其他天气API |
| 注册登录失败 | 数据库表未创建 | 检查MySQL连接，查看启动日志 |

---

## 十一、核心文件速查表

| 功能 | 文件路径 |
|------|---------|
| 安全配置 | [SecurityConfig.java](file:///d:/AGENT/ELeDangerCheck/src/main/java/org/example/eledangercheck/config/SecurityConfig.java) |
| 数据库初始化 | [DatabaseInitializer.java](file:///d:/AGENT/ELeDangerCheck/src/main/java/org/example/eledangercheck/config/DatabaseInitializer.java) |
| 图像检测主调度 | [OpenCvDetectionService.java](file:///d:/AGENT/ELeDangerCheck/src/main/java/org/example/eledangercheck/service/OpenCvDetectionService.java) |
| 图像分析服务 | [ImageAnalysisService.java](file:///d:/AGENT/ELeDangerCheck/src/main/java/org/example/eledangercheck/service/ImageAnalysisService.java) |
| YOLO检测服务 | [YoloDetectionService.java](file:///d:/AGENT/ELeDangerCheck/src/main/java/org/example/eledangercheck/service/YoloDetectionService.java) |
| 本地PPE检测 | [PpeDetectionService.java](file:///d:/AGENT/ELeDangerCheck/src/main/java/org/example/eledangercheck/service/PpeDetectionService.java) |
| 腾讯云PPE工具 | [TencentPpeDetectUtil.java](file:///d:/AGENT/ELeDangerCheck/src/main/java/org/example/eledangercheck/util/TencentPpeDetectUtil.java) |
| 天气风险服务 | [WeatherRiskService.java](file:///d:/AGENT/ELeDangerCheck/src/main/java/org/example/eledangercheck/service/WeatherRiskService.java) |
| 图像检测前端 | [image-risk.js](file:///d:/AGENT/ELeDangerCheck/src/main/resources/static/js/image-risk.js) |
| 天气前端 | [weather-risk.js](file:///d:/AGENT/ELeDangerCheck/src/main/resources/static/js/weather-risk.js) |
| 控制台前端 | [dashboard.js](file:///d:/AGENT/ELeDangerCheck/src/main/resources/static/js/dashboard.js) |
| 主配置文件 | [application.yml](file:///d:/AGENT/ELeDangerCheck/src/main/resources/application.yml) |
| MySQL配置 | [application-mysql.yml](file:///d:/AGENT/ELeDangerCheck/src/main/resources/application-mysql.yml) |

---

## 十二、版本迭代记录

| 日期 | 变更内容 |
|------|---------|
| 2026-07-15 | 项目初始版本，基础CRUD + 安全交底流程 |
| 2026-07-20 | 新增YOLOv8图像检测 + 天气风险模块 + 60条事故案例 |
| 2026-07-20 | 升级YOLO11x，修复PPE检测letterbox和数据布局问题 |
| 2026-07-21 | 接入腾讯云TIIA PPE检测（云+本地双模式），删除安全标识检测 |

---

**文档结束**

> 如有疑问，先看`PROJECT_CONFIG.md`和`AGENT_MEMORY.md`中的项目记忆文件，再查看具体代码。
